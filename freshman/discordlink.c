#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
    
    char input[151];
    
    while(fgets(input,150,stdin)){
        input[strlen(input)-1]='\0';
        
        char *ans=calloc(27,sizeof(char));
    //    if((ans=strstr(input,"http://discord.gg/")) && !)
        {
            
            puts(ans);
        }
    }
}