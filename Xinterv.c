#include<stdio.h>
#include<stdlib.h>
int Binarysearch(int arr[],int m,int g);
int main(){
    int n,m,g,i,j,cnt;
    scanf("%d %d",&n,&m);
    if(n<=100 && m<=100){
        for(cnt=0;cnt<n;cnt++){
            int arr[m];
            for(i=0;i<m;i++)
                scanf("%d",&arr[i]);
            scanf("%d",&g);
        }
    }
}
int Binarysearch(int arr[],int m,int g){
    int arr[m];
    int start=0,end=m,middle=m/2;
    while(start<=end){
        
    }
}