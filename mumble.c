#include<stdio.h>
#include<stdlib.h>
#include<string.h>
char *word;
int Mumble();
int main(){
    int lenth;
    word=(char *)calloc(50,sizeof(char));
    fgets(word,50,stdin);
    
}
int Mumble(){
    
}